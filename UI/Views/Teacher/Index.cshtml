
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container pt-5 mt-5">
    <h2>Hello @Session["DisplayName"]</h2>

    @Html.ActionLink("Upload", "Upload", "Teacher", new { role = "Teacher" }, new { @class = "btn btn-primary" })
    @Html.ActionLink("Results", "Results", "Teacher", new { role = "Teacher" }, new { @class = "btn btn-success" })
</div>
